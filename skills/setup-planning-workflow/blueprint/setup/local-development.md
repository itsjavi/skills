# Local Development Setup

This guide gets the project running from a source checkout on a contributor machine.

## What Runs Locally

Describe the local topology inferred from the repo.

```text
Developer machine
|-- app/runtime process: TBD
|-- local services: TBD
`-- external services or emulators: TBD
```

## Prerequisites

List only tools this repo appears to require.

- Runtime: TBD
- Package manager: TBD
- Database or persistence service: TBD
- Container runtime: TBD, if used
- Cloud or platform CLI: TBD, if used

## Install

Install dependencies using the repo's standard command.

```bash
TBD
```

## Environment

Create local environment files or secrets using placeholder values only. See [../ENV_VARS.md](../ENV_VARS.md) for the
full configuration catalog.

```bash
TBD
```

## Start Local Services

Start databases, queues, emulators, containers, or other local dependencies if the project needs them.

```bash
TBD
```

## Start The App

Run the local development server or process.

```bash
TBD
```

Expected local endpoints:

- App: TBD
- Health check: TBD
- Admin or debug tools: TBD

## First-Run Bootstrap

Document migrations, seed data, generated clients, first-user creation, or other required bootstrap steps.

```bash
TBD
```

## Run Checks

Use the narrowest checks available in the repo.

```bash
TBD
```

## Reset Local State

Document safe cleanup commands for local databases, volumes, generated files, caches, and test artifacts. Call out any
destructive command clearly.

```bash
TBD
```

## Troubleshooting

- **TBD:** Describe common local setup failures and fixes.
